apiVersion: v1
kind: Service
metadata:
  name: {{ include "stage6-api.fullname" . }}
  labels:
    {{- include "stage6-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stage6-api.selectorLabels" . | nindent 4 }}
